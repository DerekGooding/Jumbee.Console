# <a id="Jumbee_Console_TitleBorderStyle"></a> Enum TitleBorderStyle

Namespace: [Jumbee.Console](Jumbee.Console.md)  
Assembly: Jumbee.Console.Styles.dll  

The way a control frame title is drawn relative to the top border.

```csharp
public enum TitleBorderStyle
```

## Fields

`Inline = 0` 

Title is drawn inside the single top border row, replacing some of the border line characters.



`Double = 1` 

Title is drawn on its own row between a top border line and a separator line.



